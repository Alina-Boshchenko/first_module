spring:
  application:
    name: json_view
  datasource:
    url: jdbc:h2:mem:db
    username: root
    password: root
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: classpath:/db/changelog/changelog-master.yaml
  h2:
    console:
      enabled: true